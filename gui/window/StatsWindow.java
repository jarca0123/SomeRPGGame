package gui.window;

import gui.InteractionText;
import gui.SelectionMenu;


class StatsWindow extends Window{


    public StatsWindow(int id, int x, int y, int width, int height, boolean actionBarEnabled) {
        super(id, x, y, width, height, actionBarEnabled);

    }
}
